#
# Author: Neil Beadle
# Adds RFC2136 DDNS templates to the EdgeOS based Edge Routers

VERSION="1.2"
NAME="Dynamic DNS (DDNS) RFC 2136 integration v${VERSION}"
echo -ne "Self Extracting Installer for ${NAME}\n\n"

CDIR=$(pwd)

# Make sure script is run as root
if [[ ${EUID} -ne 0 ]]
then
	echo 'This script must be run as sudo/root!'
	exit 1
fi

if [[ -d "/var/lib/rfc2136_integration" ]]
then
	echo "${NAME} already installed, exiting..."
	exit
fi

export TMPDIR=$(mktemp -d /tmp/selfextract.XXXXXX)

ARCHIVE=`awk '/^__ARCHIVE_BELOW__/ {print NR + 1; exit 0; }' $0`

tail -n+${ARCHIVE} $0 | tar xz -C ${TMPDIR}

cd "${TMPDIR}"

./setup

cd ${CDIR}
rm -rf ${TMPDIR}

# Fix group since we have to run as root from /config/scripts/post-config.d/
# otherwise configure won't succeed when run as admin
chown -R root:vyattacfg /opt/vyatta/config/

exit 0

__ARCHIVE_BELOW__
